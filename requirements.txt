# ============================================
# FastAPI / Uvicorn (API Server)
# ============================================
fastapi==0.111.0
uvicorn[standard]==0.30.0

# ============================================
# Core Dependencies
# ============================================
pydantic==2.5.0
python-multipart==0.0.9
requests==2.31.0
typing-extensions>=4.5.0

# ============================================
# Scientific / CV Stack
# ============================================
numpy>=1.24.0,<2.0.0
opencv-python==4.9.0.80
mediapipe==0.10.11
scikit-learn==1.3.2
pandas>=2.0.0
matplotlib==3.8.4  # (선택적) 디버깅이나 시각화용
imageio==2.34.0    # (선택적) 프레임 변환 시 사용

# ============================================
# PyTorch (CPU Build)
# ============================================
torch==2.3.1+cpu
torchvision==0.18.1+cpu
torchaudio==2.3.1+cpu
# ↑ CUDA 없는 환경 (CPU 전용, PyTorch 공식 배포)

# ============================================
# Utility
# ============================================
tqdm==4.66.4
joblib==1.3.2       # scikit-learn 모델 저장/로드 시 필요
